<!-- Single source of truth is AGENTS.md. Imported below so Claude Code loads it.
     (Used instead of a symlink: Windows symlinks need Dev Mode/admin and are
     unreliable across clones; @import is portable.) -->
@AGENTS.md
